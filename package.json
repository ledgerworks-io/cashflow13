{
  "name": "@ledgerworks/cashflow13",
  "version": "0.1.0",
  "private": true,
  "description": "13-week cash flow plan — MCP server (skeleton)",
  "license": "BUSL-1.1",
  "author": "Alessandro Bonzio <info@chiriba.com>",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "node --watch --experimental-strip-types src/index.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "bundle": "npm run build && node scripts/build-mcpb.mjs",
    "release": "node scripts/release.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "exceljs": "^4.4.0",
    "express": "^5.2.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^22.15.0",
    "typescript": "^5.9.0",
    "vitest": "^4.1.10"
  }
}
